name: Build Scribe release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows:
    name: Windows x64 installer
    runs-on: windows-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build release binary
        run: cargo build --release --locked

      - name: Stage portable package
        shell: pwsh
        run: |
          $version = (Select-String -Path Cargo.toml -Pattern '^version\s*=\s*"([^"]+)"').Matches.Groups[1].Value
          if (-not $version) { throw "Could not read package version from Cargo.toml" }
          "APP_VERSION=$version" >> $env:GITHUB_ENV
          New-Item -ItemType Directory -Force -Path dist\portable | Out-Null
          Copy-Item target\release\local-transcriber.exe dist\portable\local-transcriber.exe
          Set-Content -Path dist\portable\README.txt -Value @"
          Scribe $version

          Run local-transcriber.exe to start Scribe. Models can be installed from the Models page.
          Windows x64 is the currently release-qualified platform.
          "@
          Compress-Archive -Path dist\portable\* -DestinationPath "dist\Scribe-$version-windows-x64.zip" -Force

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress --yes

      - name: Build Windows installer
        shell: pwsh
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) { throw "Inno Setup compiler not found at $iscc" }
          & $iscc "/DAppVersion=$env:APP_VERSION" installer\scribe.iss

      - name: Upload release files
        uses: actions/upload-artifact@v4
        with:
          name: scribe-windows-x64
          path: |
            dist/Scribe-*-windows-x64.zip
            dist/Scribe-Setup-*.exe
          if-no-files-found: error

      - name: Publish GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/Scribe-*-windows-x64.zip
            dist/Scribe-Setup-*.exe
